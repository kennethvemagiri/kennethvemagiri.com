# Awards images

**Drop your award/activity images here** (PNG, JPG, JPEG, WebP, GIF).

Then from the project root run:

```bash
node build-awards.js
```

Refresh the site — the strip will show all images in random order. Add or remove images anytime and run the command again.
