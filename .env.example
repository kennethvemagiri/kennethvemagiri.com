# Copy variable names into Netlify → Site configuration → Environment variables.
# For local `netlify dev`, copy to .env (never commit a real .env with secrets).

# Required for /api/github-contributions (Developer View contribution strip)
GITHUB_TOKEN=

# Optional: GitHub username whose public contribution graph to show.
# If omitted, the user authenticated by GITHUB_TOKEN is used.
GITHUB_LOGIN=
